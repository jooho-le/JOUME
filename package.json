{
  "name": "joume",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "backend/.venv/bin/uvicorn app.main:app --reload --app-dir backend",
    "dev:api:win": "backend\\.venv-win\\Scripts\\uvicorn app.main:app --reload --app-dir backend",
    "test:api": "backend/.venv/bin/pytest -q backend/tests",
    "build": "vite build",
    "preview": "vite preview",
    "test:api:win": "backend\\.venv-win\\Scripts\\pytest -q backend/tests"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "leaflet": "^1.9.4",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  }
}
